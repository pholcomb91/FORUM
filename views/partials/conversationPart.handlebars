<article class="col-12 border border-warning p-3">
    <h4>{{conver2.topic.name}}</h4>
    <blockquote>{{conver2.body}}</blockquote>
    {{#each conver2.comments as |comment|}}
    {{> comment comm=comment}}
    {{/each}}
</article>

            
